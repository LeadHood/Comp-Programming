#include "bits/stdc++.h"

using namespace std;
using ll = long long;
using ull = unsigned long long;
using p2 = pair<ll, ll>;

#define all(x) begin(x),end(x)

bool search(vector<pair<vector<int>, char>> points, vector<bool> visited, int blues, int k){
    bool found = false;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);

    int n, m, k;
    cin >> n >> m >> k;

    vector<pair<vector<int>, char>> points;
    vector<bool> visited(n);

    for(int i = 0; i < m; i++){
        char c;
        int f, t;
        cin >> c >> f >> t;

        
        //cns.push_back(connection({f, t}, c));
    }

    //search(visited, 0, k);


}
